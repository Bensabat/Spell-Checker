#pragma once

#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <tuple>

using namespace std;

tuple<string, string, string> common_prefix_decomposition(string word1, string word2);
